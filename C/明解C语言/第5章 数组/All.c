/*
    第5章 数组
        为了提高处理效率而把具有相同类型的数据有序地组织起来的一种形式——数组
    
    5-1 数组(array)
        1.数组实现相同类型的对象的集合。
        2.同一类型的变量——元素(element)集中在一起, 在内存上排列成一条直线, 这就是数组
        
        1.使用数组之前要先  声明数组
            元素类型(element type)    数组名称(array name)[元素个数(必须是常数或为空)];    // int a[5];
        2.使用数组  访问数组(通过下标/索引来访问, []下标运算符, []中的操作数为下标(subscript), 从0开始)
        

*/