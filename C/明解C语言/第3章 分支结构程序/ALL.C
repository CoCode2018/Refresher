/*
关系运算符(relational operator)
    <
    <=
    >
    >=
嵌套的if-else语句

判断：程序执行时会对控制表达式的值和类型进行检测也就是判断(evaluation)
    表达式都有值，程序执行时会对表达式的值进行判断
条件运算符(conditional operator)
    ？ ：
逻辑运算符(logical operator)
    && (AND)
    || (OR)
    ~  (NOT)
    短路求值(short circuit evaluation)
        ||，一真则真
        &&，一假则假
    
多分支选择结构
    switch语句: 通过某一单一表达式的值，将程序分为多个分支的时候，可以使用switch语句
        其中用来表示程序跳转的标识成为标签(label)，标签必须是常量
        break语句

*/